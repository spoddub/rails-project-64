databases:
  - name: rails-project-64-db
    databaseName: rails_project_64
    user: rails_project_64
    plan: free

services:
  - type: web
    name: rails-project-64
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec rails server -p $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rails-project-64-db
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
      - key: RAILS_ENV
        value: "production"
      - key: BUNDLE_WITHOUT
        value: "development:test"
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: WEB_CONCURRENCY
        value: "2"
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
